package org.synapseworks.pageharbor.image

/** Non-destructive visual treatments for one active scan page. */
enum class DocumentFilter {
    ORIGINAL,
    AUTO_ENHANCE,
    GRAYSCALE,
    BLACK_AND_WHITE,
    HIGH_CONTRAST,
}
