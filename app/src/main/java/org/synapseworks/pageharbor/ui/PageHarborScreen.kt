package org.synapseworks.pageharbor.ui

/** The current in-memory product screen; retained only across configuration changes. */
enum class PageHarborScreen {
    Home,
    ScanResult,
    OcrResult,
}
